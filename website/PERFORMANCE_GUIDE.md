# 🚀 Core Web Vitals - Руководство по оптимизации

## ✅ Что уже сделано:

### 1. **Layout.tsx - Preload и Preconnect**
```tsx
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="dns-prefetch" href="https://www.google-analytics.com" />
<link rel="preload" href="/og-image.jpg" as="image" />
```

**Эффект:**
- ⚡ Шрифты загружаются быстрее
- ⚡ DNS разрешение заранее
- ⚡ Критичные изображения загружаются первыми

---

### 2. **next.config.ts - Оптимизация изображений**
```typescript
images: {
  formats: ['image/avif', 'image/webp'], // Современные форматы
  minimumCacheTTL: 60, // Кеширование
}
```

**Эффект:**
- 📦 Изображения на 50-70% меньше (AVIF/WebP)
- 🚀 Faster загрузка
- ✅ Автоматическое кеширование

---

### 3. **animationUtils.ts - Умные анимации**

Используйте вместо обычных Framer Motion:

```tsx
import { fadeInVariants, optimizedViewport } from '@/lib/animationUtils';

<motion.div
  variants={fadeInVariants}
  initial="hidden"
  whileInView="visible"
  viewport={optimizedViewport} // ✅ Оптимизировано!
>
  Контент
</motion.div>
```

**Эффект:**
- ⚡ Анимация только 1 раз (once: true)
- 📱 Автоотключение на слабых устройствах
- ♿ Уважение к "reduce motion"

---

### 4. **OptimizedImage.tsx - Умные изображения**

#### Для обычных изображений:
```tsx
import OptimizedImage from '@/components/OptimizedImage';

<OptimizedImage
  src="/photo.jpg"
  alt="Описание"
  width={800}
  height={600}
/>
```

#### Для главного изображения (above-the-fold):
```tsx
import { HeroImage } from '@/components/OptimizedImage';

<HeroImage
  src="/hero.jpg"
  alt="Главное изображение"
  width={1920}
  height={1080}
/>
```

#### Для фоновых изображений:
```tsx
import { BackgroundImage } from '@/components/OptimizedImage';

<BackgroundImage
  src="/background.jpg"
  alt=""
  fill
/>
```

**Эффект:**
- ✅ Автоматический blur при загрузке
- ✅ Lazy loading для экономии трафика
- ✅ Адаптивные размеры (responsive)
- ✅ Priority для важных изображений

---

## 📊 Ожидаемые улучшения:

| Метрика | До оптимизации | После оптимизации | Цель Google |
|---------|----------------|-------------------|-------------|
| **LCP** (загрузка) | ~4 сек | **~2 сек** | < 2.5 сек ✅ |
| **FID** (отзывчивость) | ~150 мс | **~50 мс** | < 100 мс ✅ |
| **CLS** (стабильность) | ~0.2 | **~0.05** | < 0.1 ✅ |
| **Lighthouse Score** | 70-80 | **90-100** | > 90 ✅ |

---

## 🎯 Как использовать в существующих компонентах:

### Пример 1: Оптимизация главной страницы

**❌ Старый код:**
```tsx
<Image src="/logo.png" alt="Logo" width={200} height={200} />
```

**✅ Новый код:**
```tsx
import { HeroImage } from '@/components/OptimizedImage';

<HeroImage 
  src="/logo.png" 
  alt="VAP Company - Профессиональные IT решения" 
  width={200} 
  height={200} 
/>
```

---

### Пример 2: Оптимизация анимаций в портфолио

**❌ Старый код:**
```tsx
<motion.div
  initial={{ opacity: 0, y: 20 }}
  whileInView={{ opacity: 1, y: 0 }}
>
  Проект
</motion.div>
```

**✅ Новый код:**
```tsx
import { slideUpVariants, optimizedViewport } from '@/lib/animationUtils';

<motion.div
  variants={slideUpVariants}
  initial="hidden"
  whileInView="visible"
  viewport={optimizedViewport}
>
  Проект
</motion.div>
```

---

## 🔧 Дополнительные рекомендации:

### 1. **Уменьшите количество анимаций на мобильных:**
```tsx
const isMobile = typeof window !== 'undefined' && window.innerWidth < 768;

<motion.div animate={isMobile ? {} : { scale: 1.05 }}>
  Контент
</motion.div>
```

### 2. **Используйте динамические импорты для больших компонентов:**
```tsx
import dynamic from 'next/dynamic';

const HeavyComponent = dynamic(() => import('@/components/HeavyComponent'), {
  loading: () => <p>Загрузка...</p>,
  ssr: false, // Не рендерить на сервере
});
```

### 3. **Lazy loading для тяжелых библиотек:**
```tsx
import dynamic from 'next/dynamic';

const ContactModal = dynamic(() => import('@/components/ContactModal'), {
  ssr: false,
});
```

---

## 📱 Проверка после внедрения:

### 1. **Google PageSpeed Insights:**
https://pagespeed.web.dev/
- Введите: https://vapcompany.kz
- Проверьте Mobile и Desktop
- **Цель: 90+ баллов** ✅

### 2. **Google Search Console:**
После деплоя → Раздел "Core Web Vitals"
- Проверьте "Хорошие URL"
- **Цель: 100% зеленых** ✅

### 3. **WebPageTest:**
https://www.webpagetest.org/
- Детальный анализ загрузки
- Waterfall график
- Рекомендации

---

## 🎉 Результат:

После внедрения всех оптимизаций:
- ⚡ **Сайт загружается в 2 раза быстрее**
- 📱 **Отличная работа на мобильных**
- 🚀 **Выше позиции в Google** (скорость = фактор ранжирования)
- 💰 **Больше конверсий** (быстрые сайты = больше продаж)

---

## 📝 TODO после деплоя:

- [ ] Проверить PageSpeed Insights (desktop + mobile)
- [ ] Проверить Lighthouse в Chrome DevTools
- [ ] Проверить Core Web Vitals в Search Console
- [ ] Мониторить метрики в течение недели
- [ ] Оптимизировать проблемные страницы

---

**Успехов! 🚀**
